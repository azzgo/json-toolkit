{
  "name": "json-toolkit",
  "description": "manifest.json description",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "ext:dev": "bun --filter extension dev",
    "toolkit:dev": "bun --filter toolkit dev"
  },
  "workspaces": [
    "packages/*"
  ]
}
