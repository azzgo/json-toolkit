{
  "name": "json-toolkit",
  "description": "manifest.json description",
  "private": true,
  "version": "1.0.0-alpha.2",
  "type": "module",
  "scripts": {
    "ext:dev": "bun --filter json-toolkit dev",
    "ext:build:chrome": "bun --filter json-toolkit build:chrome",
    "ext:build:firefox": "bun --filter json-toolkit build:firefox",
    "toolkit:dev": "bun --filter toolkit dev",
    "toolkit:build": "bun --filter toolkit build",
    "toolkit:build:extension": "bun --filter toolkit build:extension"
  },
  "workspaces": [
    "packages/*"
  ]
}
